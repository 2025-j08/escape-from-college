# css

役割: スタイルシートを格納します。

説明:
- 全体のレイアウト、UI（メニュー、HUD等）、フォント定義、レスポンシブ対応のスタイルを配置します。
- ファイル例: `reset.css`（任意）, `main.css`, `components/`（小コンポーネント用）
- GitHub Pagesでの配信を想定する場合、CSSはシンプルに保ち、外部ビルドは不要にするか、ビルド手順をREADMEに明記してください。
