# asset

役割: 画像・音声・フォントなど、ゲームで使用するアセットを格納します。

説明:
- サブディレクトリ構成の例: `images/`, `audio/`, `fonts/`。
- 画像は必要に応じてPNG/WebP形式を使い、音声はOGG/MP3などブラウザ互換の形式を用意してください。
- アセットのライセンスやクレジット情報は`docs/`にまとめておくと良いです。
- ファイル名は用途が分かる命名規則を採用し、容量は必要最低限に最適化してください（読み込み性能のため）。
